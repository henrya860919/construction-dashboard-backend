---
description: 專案內 RBAC 模組權限—新增功能時如何註冊模組並串接後端／前端
---

# 專案模組權限（開發時必讀）

新增或修改**專案內**功能時：

1. 詳細步驟與動作對照見 **`docs/project-module-permissions.md`**（含 checklist）。
2. 新功能若要出現在下列 UI，必須把 `moduleId` 加入**後端與前端**的 `PERMISSION_MODULES`（**順序一致**）、前端 `PERMISSION_MODULE_LABELS`，並以 **Prisma migration 補齊** `tenant_permission_templates` 與 `project_member_permissions` 既有列（**不可只改常數**）：
   - 平台方租戶管理：**模組開通**勾選表
   - 租戶後台：**租戶資訊**唯讀開通狀態、**成員**權限範本矩陣
   - 專案內：**專案成員**模組覆寫矩陣
3. 後端：在對應 **service** 方法使用 `assertProjectModuleAction(user, projectId, moduleId, action)`；檔案上傳若經 `file.service` 類別對照，須更新該對照表與 **`docs/project-module-permissions.md`** §檔案類別。
4. 前端：專案內頁用 **`useProjectPermission(projectId)`** 的 `can(module, action)` 控制按鈕／區塊；新路由的 path 後綴須加入 `NAV_PATH_PERMISSION_MODULE`（與側欄 `pathSuffix` 一致）。

**租戶模組開通**（`tenant_module_disables`）先於細粒度：關閉之模組對該租戶所有人（含 `tenant_admin`）不可用；`platform_admin` 不檢查開通。通過後，`platform_admin`／`tenant_admin` 略過 `project_member_permissions`。前端：`platform_admin` 在 `useProjectPermission` 視為全開；`tenant_admin` 依 `my-permissions` store（已遮罩）。
